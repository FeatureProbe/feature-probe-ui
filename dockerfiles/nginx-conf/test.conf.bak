server {
  listen 8080;

  proxy_set_header Host $host;
  proxy_set_header X-Real-Ip $remote_addr;
  proxy_set_header X-Forwarded-For $remote_addr;

  location / {
    index  index.html index.htm;
    root   /workspace/feature-probe-ui/;
    try_files $uri /index.html;
  }
}